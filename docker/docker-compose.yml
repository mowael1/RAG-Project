services:

  mongodb:
    image: mongo:7-jammy
    container_name: mongodb

    ports:
      - "27007:27017"

    volumes:
      - ./mongo_data:/data/db

    restart: always

    networks:
      - backend

networks:
  backend: